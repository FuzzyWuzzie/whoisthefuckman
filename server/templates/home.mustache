<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{site.title}}</title>
  <meta property="og:title" content="{{ site.title }}" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://{{ site.root }}" />
  <meta property="og:description" content="{{ site.tag }}" />
  <meta property="og:locale" content="en_CA" />
  <meta name="twitter:card" content="summary" />
  <meta name="twitter:site" content="{{ site.twitter }}" />
  <meta name="twitter:title" content="{{ site.title }}" />
  <meta name="twitter:description" content="{{ site.tag }}" />
  <style>{{>style}}</style>
  {{>header}}
</head>
<body>
  <header>
    <h1><a href="/">{{site.title}}</a></h1>
    <p>{{site.tag}}</p>
  </header>
  <article>
    <dl>
      {{#movies}}
      <dt>{{title}}{{#year}} ({{year}}){{/year}}</dt>
      <dl>{{#actors}}<a href="/actor/{{#slugify}}{{name}}{{/slugify}}.html">{{name}}</a>{{^last}}, {{/last}}{{/actors}}</dl>
      {{/movies}}
    </dl>
  </article>
  {{>footer}}
</body>
</html>
